<h1>Listing chapters</h1>

<table class="table table-hover">
  <thead>
    <tr>
      <th>Subject</th>
      <th>#</th>
      <th>Name</th>
      <th>Term</th>
      <th>Status</th>
      <th>weightage</th>
      <th colspan="3"></th>
      <th>ID</th>
    </tr>
  </thead>

  <tbody>
    <% @chapters.each do |chapter| %>
      <tr>
        <td><%= chapter.subject.name %></td>
        <td><%= chapter.chapterNumber %></td>
        <td><%= chapter.name %></td>
        <td><%= chapter.term %></td>
        <td><%= chapter.status %></td>
        <td><%= chapter.weightage_min %>-<%= chapter.weightage_min %></td>
        <td><%= link_to 'Show', chapter %></td>
        <td><%= link_to 'Edit', edit_chapter_path(chapter) %></td>
        <td><%= link_to 'Destroy', chapter, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        <td><%= chapter.id %></td>
      </tr>
    <% end %>
  </tbody>
</table>