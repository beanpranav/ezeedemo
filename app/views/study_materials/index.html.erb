
  <h1>Listing study_materials</h1>

  <%= link_to 'New Study material', new_study_material_path %>
  <br><br>


  <table class="table table-hover table-condensed table-responsive">
    <thead>
      <tr>
        <th>Chapter</th>
        <th>Type</th>
        <th>Name</th>
        <th>Material source</th>
        <th>Model source</th>
        <th colspan="2"></th>
      </tr>
    </thead>

    <tbody>
      <% @study_materials.each do |study_material| %>
        <tr>
          <td><%= study_material.chapter.name %></td>
          <td><%= study_material.material_type %></td>
          <td><%= study_material.name %></td>
          <td><%= if study_material.video_source.length > 10 then "YES" else "MISSING" end %></td>
          <td><%= "YES" if study_material.model_source? %></td>
          <td><%= link_to 'Edit', edit_study_material_path(study_material) %></td>
          <td><%= link_to 'Delete', study_material, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
